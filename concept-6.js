const meal = 'Burgers';

if (meal === 'Rice') {
    console.log('I can eat it.');
} else if (meal === 'Burger') {
    console.log("That's good.");
} else {
    console.log('I cannot recognize this food.');
}
